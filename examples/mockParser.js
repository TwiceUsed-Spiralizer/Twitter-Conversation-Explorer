// strip everything but text and entities and rply to user id-- store in intermediary database

// gendering process -- fetches 100 oldest ungendered tweets and parses names(etc.) -- update record by id

// user population -- fetchs 100 oldest tweets without recipient info
// -- fetch from REST API and update record by id

// completion process -- fetches oldest tweets with gender
// and user fields completed and bulk indexes in elastic search
